
// Default configuration values for eightbyten

// Default fonts
#let default-fonts = (
  serif: "IBM Plex Serif",
  sans: "IBM Plex Sans",
  mono: "IBM Plex Mono",
)

// Default colors
#let default-colors = (
  text: black,
  muted: luma(60%),
)

// Default font sizes
#let default-sizes = (
  body: 9.5pt,
  h1: 28pt,
  h1-lg: 64pt,
  eyebrow: 10pt,
  small: 8pt,
)

// Default layout margins (if not calculated from paper size)
#let default-margins = (
  inner: (far: 25mm, width: 0mm, sep: 0mm),
  outer: (far: 15mm, width: 45mm, sep: 5mm),
  frontmatter: (x: 1in, y: 1in),
)
